# ========================
# NGINX API GATEWAY CONFIG
# (LOCALHOST CORS ONLY)
# ========================

events {
  worker_connections 1024;
}

http {
  sendfile on;
  keepalive_timeout 65;
  server_tokens off;

  # ------------------------
  # UPSTREAM SERVICES
  # ------------------------

  upstream auth_service {
    server system-design-app-auth-service-server-1.onrender.com:443;
  }

  upstream feed_service {
    server system-design-app-feed-service-server-1.onrender.com:443;
  }

  upstream profile_service {
    server system-design-app-profile-service-server.onrender.com:443;
  }

  # ------------------------
  # API GATEWAY SERVER
  # ------------------------

  server {
    listen 80;

    # ------------------------
    # HIDE BACKEND CORS HEADERS
    # ------------------------
    proxy_hide_header Access-Control-Allow-Origin;
    proxy_hide_header Access-Control-Allow-Credentials;
    proxy_hide_header Access-Control-Allow-Methods;
    proxy_hide_header Access-Control-Allow-Headers;

    # ------------------------
    # GLOBAL CORS HEADERS (LOCALHOST)
    # ------------------------
    add_header Access-Control-Allow-Origin http://localhost:5173 always;
    add_header Access-Control-Allow-Credentials true always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type, Authorization" always;

    # ------------------------
    # CORS PREFLIGHT (OPTIONS)
    # ------------------------
    if ($request_method = OPTIONS) {
      return 204;
    }

    # ------------------------
    # HEALTH CHECK
    # ------------------------
    location = /healthz {
      return 200 "OK\n";
    }

    # ------------------------
    # AUTH SERVICE
    # ------------------------
    location /auth/ {
      proxy_pass https://auth_service/auth/;

      proxy_set_header Host system-design-app-auth-service-server-1.onrender.com;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_redirect off;
      proxy_ssl_server_name on;
    }

    location = /auth {
      return 301 /auth/;
    }

    # ------------------------
    # FEED SERVICE
    # ------------------------
    location /feed/ {
      proxy_pass https://feed_service/feed/;

      proxy_set_header Host system-design-app-feed-service-server-1.onrender.com;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_redirect off;
      proxy_ssl_server_name on;
    }

    location = /feed {
      return 301 /feed/;
    }

    # ------------------------
    # PROFILE SERVICE
    # ------------------------
    location /profile/ {
      proxy_pass https://profile_service/profile/;

      proxy_set_header Host system-design-app-profile-service-server.onrender.com;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_redirect off;
      proxy_ssl_server_name on;
    }

    location = /profile {
      return 301 /profile/;
    }

    # ------------------------
    # FALLBACK
    # ------------------------
    location / {
      return 404;
    }
  }
}
